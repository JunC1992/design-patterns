简单工厂：
主要包括一下三个角色

Factory：工厂角色
	主要负责创建所有实例的内部逻辑
Product：抽象产品角色
	主要负责创建所有对象的父类，描述所有实例的公共接口
CreateProduct：具体产品角色
	创建具体实例

简答工程模式根据传入的参数进行对象的实例化。
使创建和业务处理分离，降低系统耦合。
系统扩展不便，每增加新的产品类型需修改工厂的代码实现
简单工厂使用静态工厂方式，工厂角色不能形成基于继承的等级结构
